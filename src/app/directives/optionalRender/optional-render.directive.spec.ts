import { OptionalRenderDirective } from './optional-render.directive';

describe('OptionalRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionalRenderDirective();
    expect(directive).toBeTruthy();
  });
});
